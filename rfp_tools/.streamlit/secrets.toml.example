# Copy this file to secrets.toml (same folder) and fill in your real key.
# secrets.toml is gitignored — it will never be committed or pushed to GitHub.
#
# For Streamlit Community Cloud, you don't need this file at all: paste the
# same line into your app's Settings -> Secrets panel in the dashboard instead.

GEMINI_API_KEY = "your-api"
